"""
Testes dos endpoints de dados:
  GET /data/chat e /data/chat/{session_id}           — listagem e detalhe de sessões
  GET /data/chat/{session_id}/insights e sub-rotas   — sentiment, topics, metrics, suggestions
  GET /data/context e /data/context/{user_id}        — perfis de usuários
  GET /data/analytics                                — agregação completa com timeline
"""
